#ifndef _REVERSE_WORD_H
#define _REVERSE_WORD_H
#include <stdio.h>

class ReverseWord
{
public:
   ReverseWord();
   virtual ~ReverseWord();
   char* strrevw(char* s);
   char* reverse_string(char* s);
};
#endif //VALID_H
